<div class="row mb-4 project">
  <div class="col-md-5">
    <h1>{{event.name}} - {{event.date}}</h1>
    <p>{{event.address}}</p>
    <ul>
      {{#each guests}}
        <li>{{this.guestName}}</li>
        <li>{{this.food}}</li>
        <li>{{this.drink}}</li>
      {{/each}}
    </ul>
  </div>
</div>